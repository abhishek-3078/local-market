const measurement=document.querySelector(".measurements")
function addMeasurement(){
    if(measurement.style.display="none")
        measurement.style.display="flex"
    else measurement.style.display="none"
}